import addProjectToList from './addProjectToList';

function getProjectIdFromName(projectTitle) {
    // if (projectTitle == "") 
    //   {ProjectIdentifier = "NoProjectTitle"}
    // else {
      // let projectIndex = addProjectToList.projectList.findIndex((el) => el.projectName === projectTitle));
      // let ProjectIdentifier = addProjectToList.projectList[projectIndex].identifier;
    // }

    // return(ProjectIdentifier)
  };

  // to reverse:
  // let taskProjectCurrentID = getProjectIdentifier(ProjectTitle);
  // let taskProjectCurrentTitle = addProjectToList.projectList.find(x => x.identifier === taskProjectCurrentID).projectName;

  export default getProjectIdFromName;